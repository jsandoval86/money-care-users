
spring:
  datasource:
    url: jdbc:h2:mem:localhost
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console

keycloak:
  client-id: app
  client-secret: d578814c-902a-405f-8c75-2426bcf19dce
  realm: money-care
  host: http://localhost:8082
  urls:
    token: /auth
    users: /users
  enabled: true